version: 2.1
jobs:
  build-and-test:
    docker:
      - image: circleci/node:current
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: yarn install
            - run: yarn run test
workflows:
  build-and-test:
    jobs:
      - build-and-test
